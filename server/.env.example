# ========================================
# PRODUCTION ENVIRONMENT VARIABLES
# ========================================

# Server Configuration
PORT=5000
NODE_ENV=production

# ========================================
# REQUIRED: MongoDB Atlas Connection String
# ========================================
# Instructions:
# 1. Go to: https://www.mongodb.com/cloud/atlas
# 2. Create or access your cluster
# 3. Click "Connect" → "Connect your application"
# 4. Copy connection string and paste below
# 5. Replace <username> and <password> with your credentials
# Format: mongodb+srv://username:password@cluster.mongodb.net/event_platform
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/event_platform

# ========================================
# REQUIRED: JWT Secret for Authentication
# ========================================
# Instructions:
# 1. Generate a random 32-character string
# 2. On Mac/Linux: openssl rand -hex 32
# 3. On Windows PowerShell: [Convert]::ToBase64String([byte[]](1..32|ForEach{Get-Random -Max 256}))
# 4. Paste the generated string below
# SECURITY: Keep this secret! Never commit to GitHub.
JWT_SECRET=your_generated_32_character_secret_here
